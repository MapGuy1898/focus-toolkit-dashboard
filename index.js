import React from "react";
import ReactDOM from "react-dom/client";
import FocusToolkitDashboard from "./FocusToolkitDashboard";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <FocusToolkitDashboard />
  </React.StrictMode>
);